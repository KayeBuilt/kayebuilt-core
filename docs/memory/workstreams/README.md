# Workstream docs

Empty until Phase 2 (WS-1 Schedule / WS-2 Ledger / WS-3 Cash&Draws / WS-4 Dashboards / WS-5 Platform), when `construction-pm` splits into parallel workstreams — see `~/dev/kayebuilt/docs/PROJECT-CONTEXT.md` §6 and `implementation-strategy.md`. `WS-*.md` skeletons get created in E6.

`/handoff` writes here once a workstream doc exists for the area a session touched; `/spinup` reads whichever one is relevant. Until then, `docs/memory/STATE.md` alone is the read order's last stop.
